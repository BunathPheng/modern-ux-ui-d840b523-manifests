---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: modern-ux-ui-d840b523                  # Ingress name
  labels:
    app: modern-ux-ui-d840b523
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "modern-ux-ui-d840b523.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: modern-ux-ui-d840b523               # Links to Service name dynamically
                port:
                  number: 3000  # Links to service port dynamically
